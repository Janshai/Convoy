os: osx
osx_image: xcode11.4
language:
    - swift
cache: cocoapods
podfile: Convoy/Podfile
install:
    - gem install xcpretty
    - gem install cocoapods --pre
    - cd Convoy
    - pod install --repo-update
script:
  - travis_wait 30 mvn install
  - travis_wait 30 mvn xcodebuild -workspace Convoy.xcworkspace -scheme Travis -sdk iphonesimulator -quiet -destination 'platform=iOS Simulator,name=iPhone X,OS=12.1' build | xcpretty
